# Simpler.Grants.gov — этот файл код не читает.
# Полный перечень переменных и Railway-блок: корневой .env.example (раздел «СЕРВИС: Simpler.Grants.gov»).

SIMPLER_ENABLED=1
# Fallback (старое имя): SIMPLER_GRANTS_GOV_ENABLED=1

SIMPLER_MAX_RECORDS_DEBUG=30
SIMPLER_DETAIL_FETCH=1
SIMPLER_INCLUDE_EXTENDED_SEARCH=0

SIMPLER_AI_ENRICH_ENABLED=0
SIMPLER_AI_MODEL=gpt-4o-mini
SIMPLER_AI_MAX_INPUT_CHARS=24000
