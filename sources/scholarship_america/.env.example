# Scholarship America — этот файл код не читает.
# Полный перечень переменных и Railway-блок: корневой .env.example (раздел «СЕРВИС: Scholarship America»).

SCHOLARSHIP_AMERICA_ENABLED=1

# Потолок успешных upsert: min(TARGET_NEW_ITEMS, N). 0 = только TARGET_NEW_ITEMS.
SCHOLARSHIP_AMERICA_MAX_RECORDS_DEBUG=0

# 1 — загружать HTML страницы стипендии; 0 — только поля с листинга.
SCHOLARSHIP_AMERICA_DETAIL_FETCH=1
